# Obsidian per-machine UI state
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/graph.json

# Generated reports / logs (machine-local), but keep the dirs
_system/lint-reports/*.json
_system/logs/*
!_system/logs/.gitkeep

# OS / tmp
.DS_Store
Thumbs.db
*.tmp

# Claude Code worktrees (embedded repos, machine-local)
.claude/worktrees/

# subagent scratch
.subagent-output/

# OKF bundle = generated, ephemeral agent-export layer（issue #3 定案 B）
# 不追蹤、不常駐；要交付給 agent 時才由 okf-exporter 現生、用完即刪
_okf/
